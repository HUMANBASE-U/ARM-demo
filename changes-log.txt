2026-05-10 21:08 (UTC+8)
- Read and followed machine usage docs:
  - C:\Users\yaoda\Desktop\HOW-TO-USE-LAB-MACHINE.txt
  - C:\Users\yaoda\Desktop\Agent-readme.txt
- Summary captured for execution:
  - Local Windows machine is for coding and Git sync only.
  - Real experiments should run on lab server via SSH.
  - Avoid dangerous/destructive server operations.
  - Record non-code environment/system changes if made.

2026-05-10 21:10 (UTC+8)
- Project initialized from scratch in ARM-demo:
  - Added README, requirements, config, src modules, and scripts.
  - Implemented end-to-end world model pipeline:
    data collection -> dataset -> model -> training -> rollout video export.
- No OS/system setting changes performed on local machine.
- No personal user file outside this project modified.

2026-05-10 21:20 (UTC+8)
- Added persistent agent-governance files based on lab workflow docs:
  - AGENTS.md
  - .cursor/rules/lab-machine-policy.mdc (alwaysApply=true)
- Purpose: enforce server-first experiment execution, timeout safety, and progress reporting.

2026-05-10 21:23 (UTC+8)
- Attempted server connectivity check:
  - ssh -o ConnectTimeout=20 ra-ugrad@trinity "pwd && ls"
  - Result: no output and timed out/hung (likely interactive auth issue in this terminal context).
  - Safety action: terminated hung process.

2026-05-10 21:24 (UTC+8)
- Improved project runtime reliability:
  - Updated training/evaluation index handling to avoid cross-dataset index reuse issues.
  - Added server one-command pipeline script: scripts/server_pipeline.sh.
  - Updated README commands for server-first workflow and corrected rollout command arguments.

2026-05-10 21:28 (UTC+8)
- Installed local helper dependency for non-interactive remote execution:
  - pip install paramiko
- Executed remote automated pipeline via scripts/remote_run.py:
  - SSH/SFTP succeeded, project files uploaded to /home/ra-ugrad/Ajax/ARM-demo.
  - Failed at dependency install because requirements used invalid package name `maniskill`.
- Fix applied:
  - Updated requirements.txt from `maniskill` -> `mani_skill`.

2026-05-10 21:32 (UTC+8)
- Re-ran remote pipeline. Dependency installation succeeded but data collection failed due:
  - NumPy 2.x ABI incompatibility with compiled extensions.
  - Missing package `pytorch_kinematics_ms` required by ManiSkill stack.
- Fix applied:
  - requirements pinned to `numpy<2` and `opencv-python<4.12`.
  - added `pytorch_kinematics_ms`.

2026-05-10 21:43 (UTC+8)
- Added new hard rules per user request:
  - Remote long-running phases must use relaxed timeout budgets (for dual-4090 lab machine).
  - If password is forgotten, consult C:\Users\yaoda\Desktop\HOW-TO-USE-LAB-MACHINE.txt first.
  - Applied in AGENTS.md and .cursor/rules/lab-machine-policy.mdc.
- Fixed server data-collection crash in scripts/collect_data.py:
  - robust conversion of torch CUDA tensors to CPU NumPy before clipping/type conversion.
- Extended scripts/remote_run.py with configurable long timeout arguments for install/collect/train/rollout stages.

2026-05-10 22:20 (UTC+8)
- Investigated "long no-response" issue:
  - Remote rollout-only job actually succeeded quickly and downloaded video.
  - Delay was caused by monitoring/wait layer, not remote execution failure.
- Prevention fix:
  - Added periodic heartbeat output in scripts/remote_run.py during each remote stage.
  - Added configurable `--heartbeat_s` to avoid silent periods in long jobs.

2026-05-14 18:15 (UTC+8)
- Added new LLM planning experiment components:
  - LLM-rules (strict planner input/output contract).
  - scripts/llm_corner_task.py (LLM high-level planning + trajectory execution + success rate + video output).
  - scripts/remote_exec.py and scripts/remote_sync.py (server command execution and code sync helpers).
  - scripts/probe_env.py (ManiSkill env introspection).
- Server-side run status:
  - First quick LLM run failed because PickCube object attr name was `cube` not `obj`.
  - Probed env attributes on server and fixed script to use `env.unwrapped.cube`.

2026-05-14 18:22 (UTC+8)
- Probed gripper control semantics on server:
  - `gripper=-1` closes fingers, `gripper=+1` opens fingers.
- Corrected LLM planner contract and runtime clamping:
  - LLM-rules updated to close<0 and open>0.
  - scripts/llm_corner_task.py updated default/clamp for `gripper_close` and `gripper_open`.

2026-05-14 18:30 (UTC+8)
- Began new architecture (forgetting JEPA) for task-level planning:
  - Added scripts/train_meta_skills.py (pure RL training for 5 meta-skills).
  - Added scripts/run_llm_meta_task.py (LLM high-level plan -> RL meta-skill execution -> success rate + videos).
  - Updated LLM-rules to strict high-level skill-sequence JSON format.
- Added infrastructure helpers:
  - scripts/remote_exec.py, scripts/remote_sync.py, scripts/remote_fetch.py.

2026-05-14 18:34 (UTC+8)
- Improved RL meta-skill training stability:
  - Added progress-shaped reward terms.
  - Increased Cartesian action step from 0.02 to 0.04.
  - Relaxed success thresholds for movement/gripper skills.
  - Aligned runtime termination thresholds with trained skill thresholds.

2026-05-14 18:39 (UTC+8)
- Further adjusted RL curriculum for movement skill generalization:
  - Move targets sampled near current EE pose (curriculum), with workspace clipping.
  - Increased max skill horizon and Cartesian step magnitude.
  - Relaxed movement success radius for faster termination learning.

2026-05-14 18:52 (UTC+8)
- Diagnosed end-to-end failure mode from logs:
  - Robot drifted with large unstable Cartesian motion during skill execution.
- Runtime stabilization updates:
  - Scaled low-level Cartesian commands.
  - Added goal-tracking blend for move_to.
  - Constrained descend/ascend to z-axis behavior.
  - Forced gripper-only skills to zero Cartesian displacement.

2026-05-14 18:57 (UTC+8)
- Added robust skill-completion fallback in runtime:
  - First half of each skill budget uses RL policy output.
  - If not completed, deterministic safety controller finishes the same skill.
  - Goal: avoid deadlock and ensure end-to-end task completion while preserving RL-first execution.

2026-05-14 19:02 (UTC+8)
- Strengthened deterministic completion phase after RL budget:
  - Added strong proportional solver for move/vertical skills in fallback phase.
  - Relaxed runtime done thresholds to reduce deadlock on marginal poses.

2026-05-14 19:48 (UTC+8)
- Switched to new single-model strategy requested by user:
  - Added scripts/train_visual_move_to.py:
    - trains one move_to actor (PPO) with required reward terms:
      - base negative distance
      - progress reward
      - per-step time penalty
      - success bonus
      - done: threshold or max steps
    - keeps gripper command fixed down during control.
    - builds visual calibration snapshots (max 3) from successful random/heuristic trials.
    - uses snapshot alignment reward as critic-style guidance.
    - saves loss curves figure and evaluation videos with target-position overlay text.

2026-05-14 19:58 (UTC+8)
- Added behavior-cloning warm start from successful heuristic pushes:
  - collect successful demo transitions while building visual snapshots.
  - pretrain actor on demo actions before PPO.
- Increased snapshot-search attempts and relaxed default success radius for evaluation.

2026-05-14 20:05 (UTC+8)
- Added heuristic-guided action blending for single-model move_to training/eval:
  - delta_cmd = 0.35 * model_delta + 0.65 * heuristic_delta
  - keeps one trained actor model while improving contact reliability and convergence.

2026-05-14 20:15 (UTC+8)
- Integrated new full pipeline with added meta-skill `grasp_cube`:
  - Added scripts/run_llm_full_pipeline.py.
  - Flow: grasp_cube -> ascend -> move_to(target) -> descend -> open_gripper.
  - `grasp_cube` uses latest trained visual actor checkpoint.
  - Saves per-episode videos, result summary, and metrics curve plot.
- Updated LLM-rules skill schema to include `grasp_cube` and new default sequence.

2026-05-14 20:33 (UTC+8)
- Adjusted full pipeline per user request:
  - Only `grasp_cube` uses guided RL policy execution until completion/max-step.
  - Remaining stages (`ascend`, `move_to`, `descend`, `open_gripper`) forced to deterministic solvers.
  - LLM now used for target-corner selection only; skill sequence fixed to 5-stage pipeline.

2026-05-14 20:40 (UTC+8)
- Added robust LLM response parsing for full pipeline:
  - strict JSON parse -> extract JSON block fallback -> keyword corner fallback.
  - Prevents whole run abort when one LLM response is malformed.

2026-05-14 20:52 (UTC+8)
- Updated full pipeline to two-meta-skill design:
  - meta skills now: `grasp_skill` and `place_skill`.
  - `grasp_skill`: guided RL visual actor, with retry loop if grasp verification fails.
  - `place_skill`: separate guided RL visual actor + deterministic descend/release tail.
  - frame capture changed to per-step (longer, readable videos), fps=10.

2026-05-14 21:22 (UTC+8)
- Updated grasp meta-skill to RL-controlled gripper timing:
  - grasp actor upgraded to 4D action output (xyz + gripper command).
  - runtime no longer hardcodes close timing; uses policy gripper output.
  - on failed grasp attempt, system immediately opens gripper and retries.

2026-05-14 21:43 (UTC+8)
- Added dedicated two-skill visual-calibration runner:
  - scripts/run_visual_calibrated_two_skill.py
  - strict two meta-skills: grasp_skill + place_skill
  - dual-view visual calibration checkpoints (max 3) for both grasp and place
  - line-trajectory attempts with retry-on-failure
  - immediate gripper-open between failed grasp retries
  - green target-circle success criterion and minimap overlay in video

2026-05-14 21:56 (UTC+8)
- Added server launcher for v5 run:
  - scripts/run_v5_remote.sh
  - activates remote conda base, checks key deps, runs v5 dual-view calibration pipeline.

2026-05-14 22:01 (UTC+8)
- Hardened remote launcher:
  - auto-installs requirements if mani_skill/torch/requests are missing in remote env.
  - keeps server-first execution resilient to env drift.

2026-05-14 22:05 (UTC+8)
- Fixed remote streaming robustness:
  - scripts/remote_exec.py now uses ASCII-safe output print to avoid GBK unicode crashes during pip progress bars.

2026-05-14 22:09 (UTC+8)
- Fixed v5 runtime calibration bug:
  - replaced `env.get_obs()` in checkpoint align with render-based dual-view extraction compatible with TimeLimitWrapper.

2026-05-14 22:22 (UTC+8)
- Upgraded visual calibration to hi-res dual-view:
  - global view resized to 256x256 and near view upgraded to 160x160.
  - near view is cube-centered, front-biased crop to emphasize gripper-cube relationship.
  - checkpoint align now requires consecutive similarity hits and uses slow approach speed.
  - remote runner now writes to outputs/llm_full_v6_hires_dual.

2026-05-16 11:18 (UTC+8)
- Added left-face-bound near-view tracking and calibrated pre-grasp alignment:
  - near-view crop tracks cube's left face relative to arm->cube viewing direction.
  - near-view binding uses temporal smoothing for stable camera lock.
  - added pre-grasp calibration stage: slow open-gripper align + visual/geometry streak check before grasp close.
  - upgraded run target to outputs/llm_full_v7_leftface_dual.
- Expanded grasp training loss reporting:
  - train_grasp_visual_skill now logs component curves: total/policy/value/entropy and reward parts (base/progress/time/done/visual).
  - run_v5_remote.sh now retrains grasp skill to checkpoints/grasp_visual_actor_v2_leftface.pt before v7 evaluation.

2026-05-16 11:37 (UTC+8)
- Reduced grasp training duration for faster full-pipeline rerun:
  - run_v5_remote.sh total_steps changed 40000 -> 24000.
  - fast outputs/checkpoints now use:
    - outputs/grasp_visual_v2_leftface_fast
    - checkpoints/grasp_visual_actor_v2_leftface_fast.pt
    - outputs/llm_full_v7_leftface_dual_fast

2026-05-17 11:04 (UTC+8)
- Enforced strict grasp timing rule:
  - in `run_visual_calibrated_two_skill.py`, gripper is forced fully open before alignment completion.
  - if pre-grasp alignment streak is not satisfied, the attempt aborts without close action.
  - checkpoint alignment during grasp also forces open gripper.
- Updated rerun profile:
  - train steps reduced to 20000.
  - new artifacts:
    - outputs/grasp_visual_v3_leftface_alignlock
    - checkpoints/grasp_visual_actor_v3_leftface_alignlock.pt
    - outputs/llm_full_v8_leftface_alignlock

2026-05-17 11:36 (UTC+8)
- Added skew-cube rotation calibration behavior in grasp execution:
  - introduced yaw error tracking (cube vs gripper) and z-axis rotation command during approach/pre-align.
  - relaxed grasp-ready criterion from perfect match to "good-enough" alignment.
  - failed attempts now apply adaptive xy calibration correction before next descent, avoiding repeated identical retries.
- Updated rerun profile and artifact names:
  - grasp retrain steps reduced to 18000.
  - outputs/checkpoint switched to:
    - outputs/grasp_visual_v4_rotcal
    - checkpoints/grasp_visual_actor_v4_rotcal.pt
    - outputs/llm_full_v9_rotcal

2026-05-17 14:24 (UTC+8)
- Bugfix for "aligned but not grasping":
  - added explicit `close_and_squeeze` stage after alignment (settle-open -> close ramp -> squeeze hold).
  - removed reliance on implicit/late close timing during lift.
  - relaxed grasp-ready threshold to "good-enough" (best visual similarity + xy geometry).
  - touch height now uses cube-relative dynamic height (`cube_z + 0.016`) for more consistent contact.

2026-05-17 14:40 (UTC+8)
- Fixed loss-curve plotting bugs:
  - `train_grasp_visual_skill.py` and `train_visual_move_to.py` now use true training-step x-axis (not 1..N index).
  - added explicit x/y axis labels across loss/reward/success plots.
- Added world-model safety guard pipeline:
  - new `scripts/train_world_model_safety.py` trains existing world model + risk head with 3-second horizon risk labels.
  - new `scripts/run_world_model_safety_demo.py` performs `Predict future -> Estimate risk -> Stop or allow` before execution.
  - added semi-transparent forbidden red zone overlay and warning stop text in output videos.
  - new `scripts/run_safety_remote.sh` runs safety training + demo end-to-end on server.

2026-05-17 17:18 (UTC+8)
- Improved grasp loss logging for dense plotting:
  - `train_grasp_visual_skill.py` now supports configurable rollout/ppo/minibatch for finer-grained step curves.
  - result.json now saves full step-wise loss series (`steps`, `loss_series`) for reliable re-plotting without retraining next time.
